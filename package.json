{
  "name": "karma-htmlmin-preprocessor",
  "version": "0.0.1",
  "description": "A karma preprocessor plugin. Minifies Html files. Useful when using other preprocessors such as karma-ng-html2js-preprocessor and testing directive rendering.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "karma-plugin",
    "karma-preprocessor",
    "preprocessor",
    "html-minifier",
    "html-min",
    "htmlmin",
    "htmlminifier",
    "minifier",
    "html",
    "min"
  ],
  "author": "Chris Liechty",
  "license": "MIT",
  "devDependencies": {
    "expect.js": "^0.3.1",
    "gulp": "^3.9.0",
    "gulp-mocha": "^2.1.1",
    "mocha": "^2.2.5"
  },
  "dependencies": {
    "html-minifier": "^0.7.2"
  }
}
